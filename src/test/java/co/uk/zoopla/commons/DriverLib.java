package co.uk.zoopla.commons;

import org.openqa.selenium.WebDriver;

public class DriverLib
{
    //I am importing a variable called driver, type is Webdriver. I don't have to create its object to use it
    public static WebDriver driver;
}
